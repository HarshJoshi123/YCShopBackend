const ProductRoutes = require('./product');
const OrderRoutes = require('./order');
const UserRoutes = require('./user');
const CartRoutes = require('./order');

module.exports = {
    ProductRoutes,
    OrderRoutes,
    UserRoutes,
    CartRoutes
}
